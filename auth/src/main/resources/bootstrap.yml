server:
  port: 8889

spring:
  application:
    name: dubbo-web-auth
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      server-addr: localhost:8848
      username: nacos
      password: nacos

